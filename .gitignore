# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Env files — never commit .env (it may hold secrets). The committed template is
# .env.example; copy it to .env to override the defaults locally.
.env
.env.*
!.env.example
# Docker runtime env (secrets like COUNTER_IP_SALT); .example is committed
docker/*.env
!docker/*.env.example

# Runtime counter data (do not commit; contains hashed visitor data)
server/data/counter.json
